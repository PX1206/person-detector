# 服务
HOST=0.0.0.0
PORT=8090

# 模型
MODEL_NAME=yolo11m.pt
IMGSZ=640
CONF_THRESHOLD=0.35
MIN_BOX_AREA_RATIO=0.001

# CPU 线程数（4 核服务器建议设为 4）
TORCH_THREADS=4

# 拉取报警图片超时（秒）
IMAGE_FETCH_TIMEOUT=10
# Docker 内优先 IPv4 拉图，避免 IPv6 握手超时（宿主机 curl 快、容器 httpx 慢时开启）
IMAGE_FETCH_PREFER_IPV4=1
